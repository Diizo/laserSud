// This file adds metadata for the contact page
// The actual page is in page.tsx (client component)
// Next.js 13+ allows metadata only in Server Components

// Note: since contact/page.tsx is a client component ("use client"),
// you can add title via the <title> tag directly or via a parent layout.
// Here is the recommended pattern: wrap the client form in a server component.
